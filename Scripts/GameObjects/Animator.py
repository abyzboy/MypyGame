class Animator:
    def __init__(self, images_with_dict):
        self.images_with_dict = images_with_dict

    def play(self, name_anim):
        if self.images_with_dict[name_anim]:
            pass
        return
